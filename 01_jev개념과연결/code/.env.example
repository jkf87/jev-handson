# console.typesafe.ai/keys 에서 발급한 키. 이 파일을 .env로 복사해 값을 넣고, 절대 커밋하지 않는다.
TYPESAFE_API_KEY=
# 기본값 jev-latest(현재 jev-1.13.0). 비교 실험 때는 정확한 버전을 고정한다.
TYPESAFE_DEFAULT_MODEL=jev-latest
# 로컬 호환 서버(3강 decider 등)를 쓸 때만 바꾼다
# TYPESAFE_BASE_URL=http://127.0.0.1:8000
